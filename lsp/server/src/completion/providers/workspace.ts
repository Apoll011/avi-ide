// providers/workspace.ts
import { CompletionItem } from "vscode-languageserver/node";

export function workspaceCompletions(): CompletionItem[] {
  // TODO:
  // - Parse documents
  // - Extract functions, variables, types
  // - Return CompletionItems
  return [];
}
